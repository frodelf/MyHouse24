name: Build & Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Git Repo
        uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'adopt'
      - name: Build the project
        run: |
          mvn package -DskipTests=true
      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: OkeNNV@gmail.com
          password: Uragan85554
      - name: Docker Build and Push to DockerHub
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: okennv/myhouse24
      - name: Connect with SSH and Deploy
        uses: appleboy/ssh-action@master
        with:
          host: slj.avada-media-dev2.od.ua
          username: slj
          password: OWT3ztRrEmbw58W1
          script: |
            sudo docker stop myhouse24-admin-DA
            sudo docker rm myhouse24-admin-DA
            sudo docker rmi okennv/myhouse24
            sudo docker login
            sudo docker login -u OkeNNV@gmail.com -p Uragan85554
            sudo docker pull okennv/myhouse24:latest
            sudo docker run -d --name myhouse24-admin-container -p 8088:443 --network myhouse24da -v myhouse24da:/var/photos okennv/myhouse24
